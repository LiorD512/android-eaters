<?xml version="1.0" encoding="utf-8"?>
<animated-selector xmlns:android="http://schemas.android.com/apk/res/android">
        <item   android:id="@+id/normal"
                android:state_selected="false" android:drawable="@drawable/circle_white_grey"/>
        <item   android:id="@+id/selected"
                android:state_selected="true" android:drawable="@drawable/circle_teal_blue"/>

    <transition
            android:fromId="@+id/normal"
            android:toId="@+id/selected" >
        <animation-list>
            <!--fill in your animation here-->
        </animation-list>
    </transition>
</animated-selector>